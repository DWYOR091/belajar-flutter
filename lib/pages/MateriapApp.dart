import 'package:flutter/material.dart';

class CobaMateriealApp extends StatelessWidget {
  const CobaMateriealApp({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
